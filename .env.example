# TradeUpSPY environment template
# Copy as .env and fill values as needed

# CSFloat API
CSFLOAT_API_KEY=
CSFLOAT_BASE=https://csfloat.com
# Alternative var name supported by cs2_local_prices
CSFLOAT_API_BASE=https://csfloat.com
# Optional: send browser session cookie when needed ("session=...; cf_clearance=...")
CSFLOAT_COOKIE=
# Optional: auth style for cs2_local_prices (bearer|raw|both)
CSFLOAT_AUTH_STYLE=both
